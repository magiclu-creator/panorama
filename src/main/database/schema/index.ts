// Re-export all schema tables
// Drizzle ORM schema definitions for type-safe queries

export const schema = {
  // This file serves as the entry point for schema imports
  // Individual table schemas will be added in Phase 1
}
